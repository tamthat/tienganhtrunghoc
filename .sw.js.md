# public/sw.js — bộ nhớ

> Service worker của PWA. Được Vite chép nguyên xi sang `dist/`, rồi **`scripts/build-sw.mjs` đóng dấu lại**.

## Chức năng
Cho app cài được như ứng dụng và mở được khi mất mạng.

## Cấu trúc
- `VERSION` + `PRECACHE`: **hai dòng placeholder**, bị `scripts/build-sw.mjs` thay sau mỗi lần build.
  Giá trị trong mã nguồn (`'dev'`, `['./index.html']`) chỉ là chỗ giữ chỗ — **đừng sửa tay để "cập nhật"**.
- `install`: precache **từng file một** trong vòng lặp `for` — một file lỗi không được làm hỏng cả mẻ
  (khác `cache.addAll`, hỏng một là hỏng hết). Rồi `skipWaiting()`.
- `activate`: xoá mọi cache tên `english-*` không nằm trong `KEEP`, rồi `clients.claim()`.
- `fetch`: bỏ qua non-GET và khác origin → `navigate` network-first → `dict/grammar` cache-first ở
  `DATA_CACHE` → còn lại cache-first ở `SHELL_CACHE`.

## Ràng buộc & quyết định
- **`DATA_CACHE = 'english-data-v1'` cố tình KHÔNG gắn `VERSION`.** `public/dict` nặng ~35MB / 769
  file; xoá theo mỗi bản deploy là bắt học sinh tải lại hàng chục MB. Nội dung từ điển không đổi.
  ⇒ Muốn dọn nó phải **tự tay đổi số `v1`**.
- Bỏ qua **khác origin** là bắt buộc: proxy CORS và API từ điển mà bị cache sẽ trả kết quả sai/cũ.
- Mọi đường dẫn resolve qua `new URL(path, self.registration.scope)` vì `vite.config.ts` đặt
  `base: './'` (deploy được cả ở gốc lẫn sub-path).
- `skipWaiting` + `clients.claim`: bản mới áp dụng ngay. An toàn vì asset có tên đã hash — trang cũ
  vẫn lấy đúng file cũ theo URL.

## ⛔ Sai lầm đã mắc
- **Bản đầu chỉ precache `index.html`** và định tuyến `/assets/` sang một cache khác (`ASSET_CACHE`)
  mà không hề nạp gì vào đó ⇒ offline **không** thật sự chạy bằng SW, chỉ nhờ HTTP cache của trình
  duyệt. Đã gộp asset vào `SHELL_CACHE` và precache đầy đủ.
- Kiểm offline lần đầu **không tắt HTTP cache** → thấy "chạy được" và suýt kết luận sai. Xem cách
  kiểm đúng ở [[pwa]].

## ✅ Cải tiến đã áp dụng
- Precache từng file (`for` + `try`) thay vì `addAll`.
- `VERSION` băm từ danh sách tên file → build lại mà output không đổi thì **không** xoá cache oan.

## Liên quan
`scripts/.build-sw.mjs.md` · `src/.main.tsx.md` · [[pwa]]
